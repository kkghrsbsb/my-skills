# Agent Development Flow Entry Point

Read [AGENTS.md](AGENTS.md) for this project's workflow routing, then load the relevant skill and shared protocol by their file paths. Use the host's available capabilities; a Codex task API or a specific slash command is not required. Keep existing project-specific Claude instructions when adopting this entry point.
